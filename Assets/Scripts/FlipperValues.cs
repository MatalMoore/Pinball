using UnityEngine;

namespace FlipperValues {
    public struct Values {
        public enum FlipperState {
            down,
            up,
            goingDown,
            goingUp
        }
    }
}
